import { Container } from "@mui/material"

function Contacts() {
    return (
        <Container maxWidth={false}>
            <div>Contacts</div>
        </Container>
    )
}

export default Contacts